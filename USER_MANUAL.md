# 使用手册 (User Manual)

> **法律案件分析系统 — Alpha 版**

---

## 目录

1. [Legal Assist 主程序](#1-legal-assist-主程序)
   - [启动与界面](#11-启动与界面)
   - [案情输入与事实提取](#12-案情输入与事实提取)
   - [罪名分析](#13-罪名分析)
   - [设置](#14-设置)
2. [Embedding Builder Tool 离线工具](#2-embedding-builder-tool-离线工具)
   - [YAML 基座编辑](#21-yaml-基座编辑)
   - [Embedding 计算](#22-embedding-计算)
3. [常见问题](#3-常见问题)

---

## 1. Legal Assist 主程序

### 1.1 启动与界面

启动程序后，您将看到公安风格的深蓝色界面，左侧/底部为导航栏（桌面端侧边导航，移动端底部导航）。

**主要页面：**

- **案情输入**：输入案件描述，提取事实要素。
- **分析结果**：查看罪名匹配结果和详细分析。
- **设置**：配置 LLM API 和阈值参数。

**状态栏 (右上角)：**

- `YAML: X slots` — 已加载的事实要素槽位数量。
- `Embedding: X` — 已加载的向量数量。
- 绿色表示加载成功，红色表示加载失败。

---

### 1.2 案情输入与事实提取

1. 在"案情描述"文本框中输入案件自然语言描述。

   **示例：**

   ```
   2023年5月，被告人张某在某自然保护区内使用猎夹捕获国家二级保护动物穿山甲2只，
   后在网上以每只5000元的价格出售。张某到案后如实供述犯罪事实。
   ```

2. 点击 **"Phase 2: 提取事实"** 按钮。

3. 系统将调用 LLM 自动提取结构化事实要素，结果显示在右侧"提取结果"区域。

> **注意**：此步骤需要联网并配置有效的 API Key。

---

### 1.3 罪名分析

1. 事实提取完成后，点击 **"Phase 3: 分析罪名"** 按钮。

2. 系统将使用本地向量匹配算法，在离线状态下完成分析。

3. **结果解读：**
   - **罪名列表**：按匹配度排序，显示"可能构成"或"不构成"。
   - **详细分析**：点击罪名展开，查看：
     - `匹配度`：总体相似度百分比。
     - `命中的必需要件`：已满足的法定构成要素。
     - `缺失的必需要件`：未满足的构成要素（如有）。
     - `分析说明`：法律条文引用和解释。

---

### 1.4 设置

**LLM 配置：**

- `API Key`：阿里云 DashScope API 密钥。
- `Chat 模型`：用于事实提取的模型（默认 qwen-max）。
- `Embedding 模型`：用于向量计算的模型（默认 text-embedding-v4）。
- `Embedding 维度`：向量维度（默认 1024）。

**分析参数：**

- `相似度阈值`：判定"构成"的最低匹配度（默认 0.45，范围 0.3-0.9）。

**缓存控制：**

- `清除配置`：重置所有设置为默认值。

---

## 2. Embedding Builder Tool 离线工具

> 此工具供**维护人员**使用，用于管理 YAML 基座和生成 Embedding 包。

### 2.1 YAML 基座编辑

1. **新建模板**：点击"新建模板"创建默认 YAML 结构。
2. **打开文件**：加载现有的 `legal_base.yaml` 文件。
3. **编辑 Slots**：添加/删除事实要素槽位，设置名称、语义边界、分析层级。
4. **编辑 Crimes**：添加/删除罪名定义，配置必需/可选/排除要件。
5. **保存**：导出修改后的 YAML 文件。

### 2.2 Embedding 计算

1. 确保已加载 YAML 基座。
2. 在"设置"页面配置 LLM API Key 和 Embedding 模型。
3. 选择法律条文目录（包含 `.txt` 格式的法条文件）。
4. 点击"开始计算"，等待进度完成。
5. 点击"导出 .pak 文件"保存 Embedding 包。

> 生成的 `.pak` 文件需复制到 `legal_assist/assets/` 目录并更新 `pubspec.yaml`。

---

## 3. 常见问题

### Q: 启动时显示"加载 Embedding 包失败"？

**A:** 检查 `assets/` 目录下是否存在 `.pak` 文件，确认 `pubspec.yaml` 中路径正确。

### Q: "提取事实"按钮无响应？

**A:** 确认已在设置中配置有效的 API Key，并检查网络连接。

### Q: 分析结果全部显示"不构成"？

**A:** 尝试降低"相似度阈值"设置（如从 0.45 调整到 0.35）。

### Q: Windows 运行时报 MissingPluginException？

**A:** 执行 `flutter clean` 后重新运行 `flutter run`。如问题持续，尝试显式添加 `shared_preferences_windows` 和 `path_provider_windows` 到 `pubspec.yaml`。

---

*本手册适用于 Alpha 版本，内容可能随版本更新而变化。*
